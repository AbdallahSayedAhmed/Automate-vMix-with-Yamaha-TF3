# ============================================================
# vMix ↔ Yamaha TF3 Bridge — Backend Dependencies
# ============================================================
# Core ASGI Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Async Database (SQLite)
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0

# Async HTTP Client (vMix Web API)
httpx>=0.27.0

# WebSocket support (bundled with uvicorn[standard])
websockets>=13.0

# Settings / Environment
pydantic-settings>=2.6.0

# XML Parsing (vMix API responses)
lxml>=5.3.0
